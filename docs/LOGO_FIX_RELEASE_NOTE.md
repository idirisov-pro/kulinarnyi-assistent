# Logo fix

The app header and public metadata previously used a manually recreated SVG that did not match the approved Product Radar logo.

This release replaces that reconstruction with a contour traced directly from the approved master asset and bumps the brand cache key from `brand2` to `brand3` so browsers request the corrected mark.
