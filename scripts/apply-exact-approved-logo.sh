#!/usr/bin/env bash
set -euo pipefail

# This script is intentionally kept in the branch only as a provenance note.
# The actual approved raster files are committed directly as binary blobs.
# Source of truth: the exact Product Radar-approved logo asset, not a redrawn SVG.
